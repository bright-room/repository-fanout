# P5: CLI（手動 reconcile / dry-run）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `apps/cli` — `@repository-fanout/core` を使い、(a) 1リポの **dry-run**（出す予定の差分を表示）と (b) **手動 reconcile**（実際に PR 作成）を Node で実行できる CLI。Cloudflare 障害時のフォールバック・調査用（spec §16-5）。

**Architecture:** worker の child workflow と同じ core ロジックを使うが、orchestration は単純な逐次実行。`GitHubTemplateSource`/`RepoIO` は P2 と同型のものを CLI 用に再利用（core に寄せるのが理想だが、まずは最小複製で可）。

**Tech Stack:** TypeScript, Node 20+（グローバル fetch/crypto）, vitest。

**前提:** P1 完了。P2 の `templateSource.ts`/`repoIO.ts` を参考にする（共通化は将来）。

---

## Task 1: cli 雛形

**Files:**
- Create: `apps/cli/package.json`, `apps/cli/tsconfig.json`, `apps/cli/src/index.ts`

- [ ] **Step 1: パッケージ**

`apps/cli/package.json`:
```json
{
  "name": "@repository-fanout/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "fanout": "./src/index.ts" },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit", "start": "node --experimental-strip-types src/index.ts" },
  "dependencies": { "@repository-fanout/core": "workspace:*" },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0", "@types/node": "^22.0.0" }
}
```

`apps/cli/tsconfig.json`:
```json
{ "extends": "../../tsconfig.base.json", "compilerOptions": { "rootDir": "src", "types": ["node"], "lib": ["ES2022"] }, "include": ["src", "test"] }
```

- [ ] **Step 2: Commit**

```bash
git add apps/cli/package.json apps/cli/tsconfig.json
git commit -m "chore(cli): scaffold cli package"
```

---

## Task 2: reconcile プランナー（純関数・dry-run の中身）

**Files:**
- Create: `apps/cli/src/planRepo.ts`
- Test: `apps/cli/test/planRepo.test.ts`

core の `resolveDesiredFiles` + `computeChanges` を束ね、「どのファイルが追加/更新されるか」を返す純関数。TemplateSource と「実ファイル取得関数」を注入してテスト。

- [ ] **Step 1: 失敗するテストを書く**

`apps/cli/test/planRepo.test.ts`:
```ts
import { expect, test } from "vitest";
import { planRepo } from "../src/planRepo.js";
import type { TemplateSource } from "@repository-fanout/core";

const source: TemplateSource = {
  async readFile(p) { return p === "base/files/renovate.json" ? '{"extends":[{{renovate_extends}}]}' : null; },
  async listFiles(prefix) { return prefix === "base/files/" ? ["base/files/renovate.json"] : []; },
  async readProfileManifest(dir) { return dir === "base" ? { renovate: ["github>o/c//presets/default"] } : null; },
  async profileExists() { return true; },
};

test("planRepo reports changes vs actual", async () => {
  const plan = await planRepo({
    source, profiles: [], vars: {}, exclude: [],
    readActual: async () => ({}), // 何も無い → 追加
  });
  expect(plan.changes.map((c) => c.path)).toEqual(["renovate.json"]);
  expect(plan.changes[0].content).toBe('{"extends":["github>o/c//presets/default"]}');
});

test("planRepo no-op when actual matches", async () => {
  const plan = await planRepo({
    source, profiles: [], vars: {}, exclude: [],
    readActual: async () => ({ "renovate.json": '{"extends":["github>o/c//presets/default"]}' }),
  });
  expect(plan.changes).toEqual([]);
});
```

- [ ] **Step 2: テスト失敗を確認**

Run: `pnpm --filter @repository-fanout/cli test planRepo`
Expected: FAIL

- [ ] **Step 3: 実装**

`apps/cli/src/planRepo.ts`:
```ts
import { resolveDesiredFiles, computeChanges, type TemplateSource, type FileChange } from "@repository-fanout/core";

export interface PlanArgs {
  source: TemplateSource;
  profiles: string[];
  vars: Record<string, string>;
  exclude: string[];
  readActual: (paths: string[]) => Promise<Record<string, string>>;
}

export async function planRepo(args: PlanArgs): Promise<{ changes: FileChange[] }> {
  const desired = await resolveDesiredFiles({
    source: args.source, profiles: args.profiles, vars: args.vars, exclude: args.exclude,
  });
  const actual = await args.readActual(desired.map((d) => d.path));
  return { changes: computeChanges(desired, actual) };
}
```

- [ ] **Step 4: テスト通過を確認**

Run: `pnpm --filter @repository-fanout/cli test planRepo`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/cli/src/planRepo.ts apps/cli/test/planRepo.test.ts
git commit -m "feat(cli): repo plan (dry-run core)"
```

---

## Task 3: CLI エントリ（dry-run）

**Files:**
- Create: `apps/cli/src/index.ts`, `apps/cli/src/github.ts`

`fanout dry-run --repo owner/name --profiles terraform,typescript` で、common-files を読み対象リポの差分を表示。GitHub トークンは `GITHUB_TOKEN` 環境変数（または App トークン）から。

- [ ] **Step 1: GitHub アクセス（P2 の templateSource/repoIO を簡易移植）**

`apps/cli/src/github.ts`:
```ts
import { GitHubClient, type ProfileManifest, type TemplateSource } from "@repository-fanout/core";

export function templateSource(client: GitHubClient, repo: string): TemplateSource {
  let treeCache: Promise<string[]> | undefined;
  const tree = () => (treeCache ??= client
    .request<{ tree: Array<{ path: string; type: string }> }>("GET", `/repos/${repo}/git/trees/HEAD?recursive=1`)
    .then((r) => r.tree.filter((t) => t.type === "blob").map((t) => t.path)));
  const read = async (path: string) => {
    try {
      const r = await client.request<{ content: string }>("GET", `/repos/${repo}/contents/${encodeURI(path)}`);
      return atob(r.content.replace(/\n/g, ""));
    } catch { return null; }
  };
  return {
    async readFile(p) { return read(p); },
    async listFiles(prefix) { return (await tree()).filter((p) => p.startsWith(prefix)); },
    async readProfileManifest(dir) { const raw = await read(`${dir}/profile.json`); return raw ? (JSON.parse(raw) as ProfileManifest) : null; },
    async profileExists(tag) { return (await tree()).some((p) => p.startsWith(`profiles/${tag}/`)); },
  };
}

export function actualReader(client: GitHubClient, repo: string, ref = "HEAD") {
  return async (paths: string[]) => {
    const out: Record<string, string> = {};
    for (const path of paths) {
      try {
        const r = await client.request<{ content: string }>("GET", `/repos/${repo}/contents/${encodeURI(path)}?ref=${ref}`);
        out[path] = atob(r.content.replace(/\n/g, ""));
      } catch { /* missing */ }
    }
    return out;
  };
}
```

- [ ] **Step 2: index.ts（最小引数パース + dry-run 出力）**

`apps/cli/src/index.ts`:
```ts
import { GitHubClient } from "@repository-fanout/core";
import { templateSource, actualReader } from "./github.js";
import { planRepo } from "./planRepo.js";

function arg(name: string): string | undefined {
  const i = process.argv.indexOf(`--${name}`);
  return i >= 0 ? process.argv[i + 1] : undefined;
}

async function main() {
  const cmd = process.argv[2];
  const repo = arg("repo");
  const templatesRepo = arg("templates") ?? "bright-room/common-files";
  const profiles = (arg("profiles") ?? "").split(",").filter(Boolean);
  const codeowner = arg("codeowner") ?? repo?.split("/")[0] ?? "";
  const token = process.env.GITHUB_TOKEN;
  if (cmd !== "dry-run" || !repo || !token) {
    console.error("usage: GITHUB_TOKEN=... fanout dry-run --repo owner/name [--profiles a,b] [--templates owner/repo] [--codeowner x]");
    process.exit(2);
  }
  const client = new GitHubClient({ token });
  const plan = await planRepo({
    source: templateSource(client, templatesRepo),
    profiles, vars: { codeowner }, exclude: [],
    readActual: actualReader(client, repo),
  });
  if (plan.changes.length === 0) { console.log(`${repo}: no changes (in sync)`); return; }
  console.log(`${repo}: ${plan.changes.length} file(s) would change:`);
  for (const c of plan.changes) console.log(`  ~ ${c.path}`);
}
main().catch((e) => { console.error(e); process.exit(1); });
```

- [ ] **Step 3: 手動確認**

Run: `GITHUB_TOKEN=<pat> pnpm --filter @repository-fanout/cli start dry-run --repo bright-room/endpoint-gate --profiles terraform`
Expected: 変更予定ファイル一覧 or "no changes"

- [ ] **Step 4: typecheck & commit**

```bash
pnpm --filter @repository-fanout/cli typecheck
git add apps/cli/src
git commit -m "feat(cli): dry-run command"
```

---

## Task 4（任意）: apply モード

**Files:**
- Modify: `apps/cli/src/index.ts`

- [ ] **Step 1:** `fanout apply --repo ...` で、planRepo の changes を P2 `RepoIO` 相当でコミット＆PR 作成（child workflow と同じ手順を逐次実行）。core は同じ、I/O は github.ts に commit/PR を足す。Cloudflare 障害時の手動リカバリ用。
- [ ] **Step 2:** dry-run で確認 → apply で1リポに PR を作れることを手動検証。
- [ ] **Step 3: Commit**

```bash
git commit -am "feat(cli): apply mode for manual reconcile"
```

---

## Self-Review

- **Spec カバレッジ**：§16-5 フォールバック dry-run/手動 reconcile = Task3,4 / core 再利用（runtime 非依存の利点）= 全タスク。
- **DRY 注意**：templateSource/actualReader は P2 と重複。将来 core へ「TemplateSource ファクトリ」を移して共有するのが望ましい（YAGNI のため MVP は複製、Self-Review で明記）。
- **整合**：`planRepo` は core の `resolveDesiredFiles`/`computeChanges` をそのまま使用＝worker と同一判定。

# P6: v0（GitHub Actions 配布）撤去 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** repository-fanout（P1–P4）が本番稼働し配布が確認できた後、旧 v0（`distribute-initial-files` 系）を `organization-structure` / `kukv/structure` から安全に撤去する。

**Architecture:** カットオーバー。新システムが全対象リポを収束できていることを確認してから削除する（ロールバック可能な順序）。spec §10 フェーズ5。

**Tech Stack:** GitHub Actions（削除）, bash スクリプト（削除）。

**前提:** P4 完了・本番で新フローによる配布が確認済み（少なくとも全対象リポで1巡 reconcile 成功）。

---

## Task 1: 新フローの網羅性を確認（撤去の前提）

**Files:** なし（確認のみ）

- [ ] **Step 1:** 全対象リポで新フローの reconcile が成功していることを RUNS KV（または `wrangler tail`）で確認。
- [ ] **Step 2:** v0 が配っていた3ファイル（renovate.json / CODEOWNERS / release.yml）が、新フローでも各リポに存在することをスポットチェック（数リポ）。
- [ ] **Step 3:** 差異があれば P3/P4 を直してから本撤去に進む（ここで停止可能）。

Expected: 新フローが v0 の配布範囲をカバーしている確証。

---

## Task 2: organization-structure から v0 を削除

**Files（organization-structure）:**
- Delete: `.github/workflows/distribute-initial-files.yml`
- Delete: `.github/scripts/distribute-initial-files.sh`
- Delete: `.github/scripts/trigger-distribute-for-new-repos.sh`
- Delete: `.github/config/exclude-repos.json`
- Delete: `.github/templates/` 配下（renovate.json.template / release.yml.template / CODEOWNERS.template）
- Modify: `.github/workflows/on-merge.yml`（v0 trigger 呼び出しがあれば除去。P4 で入れた fanout-sync は残す）

- [ ] **Step 1: v0 ファイル削除**

```bash
cd organization-structure
git rm .github/workflows/distribute-initial-files.yml \
       .github/scripts/distribute-initial-files.sh \
       .github/scripts/trigger-distribute-for-new-repos.sh \
       .github/config/exclude-repos.json
git rm -r .github/templates
```

- [ ] **Step 2: on-merge.yml から v0 トリガー step を除去**

`on-merge.yml` に `trigger-distribute-for-new-repos.sh` を呼ぶ step が残っていれば削除。**P4 の "Trigger fanout sync" step は残す。**

- [ ] **Step 3: Actions allowlist の掃除（任意）**

`repository_organization_structure.tf`（administrator）の `actions_permissions.patterns_allowed` から、v0 でのみ使っていた `peter-evans/create-pull-request@*` が他で不要なら削除を検討（fanout は Worker 側で PR を作るため Actions の create-pull-request は不要になる）。

- [ ] **Step 4: PR → レビュー → マージ**

```bash
git commit -m "chore: remove v0 distribute-initial-files (superseded by repository-fanout)"
```

Expected: CI（fmt/validate/plan）通過。新規リポ作成時の配布が **fanout 経由のみ**になる。

---

## Task 3: kukv/structure から v0 を削除

**Files（kukv/structure）:**
- Delete: `.github/workflows/distribute-initial-files.yml`
- Delete: `.github/scripts/distribute-initial-files.sh`
- Delete: `.github/scripts/trigger-distribute-for-new-repos.sh`
- Delete: `.github/config/exclude-repos.json`
- Delete: `.github/templates/`（renovate.json.template）
- Modify: `.github/workflows/on-merge.yml`

- [ ] **Step 1: 削除**

```bash
cd ../structure   # kukv/structure
git rm .github/workflows/distribute-initial-files.yml \
       .github/scripts/distribute-initial-files.sh \
       .github/scripts/trigger-distribute-for-new-repos.sh \
       .github/config/exclude-repos.json
git rm -r .github/templates
```

- [ ] **Step 2: on-merge.yml 整理（P4 の fanout-sync は残す）**

- [ ] **Step 3: PR → マージ**

```bash
git commit -m "chore: remove v0 distribute-initial-files (superseded by repository-fanout)"
```

---

## Task 4: 撤去後の最終確認

- [ ] **Step 1:** organization-structure に新規ダミーリポを `profiles=["terraform"]` で作成 → merge。
- [ ] **Step 2:** fanout が新規リポに初期 PR を作成することを確認（v0 不在でも配布される）。
- [ ] **Step 3:** ダミーリポを Terraform から削除して片付け。

Expected: v0 無しで新規配布が成立。撤去完了。

---

## Self-Review

- **Spec カバレッジ**：§1 v0 置換完了 / §10 フェーズ5 撤去 = Task2,3。
- **安全性**：Task1 で網羅性を確認してから削除（ロールバック可能順）。fanout-sync step（P4）は誤って消さないよう Task2-2/3-2 で明示。
- **副作用**：Actions allowlist の `peter-evans/create-pull-request` は fanout が Worker で PR 作成するため不要化しうる（Task2-3 で掃除を任意提示）。

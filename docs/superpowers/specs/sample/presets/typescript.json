{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "TypeScript / Node リポ向けの追加設定（例）。@types を本体とまとめる、pnpm の lockfile を扱う等。",
  "packageRules": [
    {
      "description": "@types/* を対応する本体パッケージと同じ PR にまとめる",
      "matchPackagePatterns": ["^@types/"],
      "groupName": "type definitions"
    }
  ]
}
